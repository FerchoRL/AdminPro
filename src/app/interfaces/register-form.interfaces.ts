export interface RegisterForm {
    //Son los campos que lleva mi formulario de registro en el frontend
    userName: string;
    email: string;
    password: string;
    password2: string;
    terms: boolean;
}